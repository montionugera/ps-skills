# Storyboard

One row per section of app/content.md. `question` is one of the brief's
Q1-Q3 (every one must appear at least once). `facts` cites one or more fact
ids from 01-facts.md (comma-separated).

| section | question | facts |
| --- | --- | --- |
| 1 | (...) | (...) |
