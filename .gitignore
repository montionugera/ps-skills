# Python
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
.venv/

# Node (skill templates ship source only; builds are generated)
node_modules/
dist/

# Skill runtime artifacts
**/server.log

# OS
.DS_Store

# Machine-local engine files: they name private repos/paths, so they stay out of
# this public repo. They still live in the working tree, so the symlinked
# ~/.claude/ps-release-workflow keeps them.
engine/ps-release-workflow/verify.sh
engine/ps-release-workflow/docs/superpowers/

# ps-skills-sync state
.sync/
